FROM python:3.12

# Устанавливаем рабочую директорию внутри контейнера
WORKDIR /server

# Копируем код сервера в рабочую директорию
COPY server.py .

# Открыл порт, на котором будет работать сервер
EXPOSE 8080

# Запустил сервер
CMD ["python", "server.py"]
